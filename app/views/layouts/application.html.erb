<!DOCTYPE html>
<html>
  <head>
    <title>RailsCi</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-50">
    <header class="border-b border-slate-200 bg-white">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <div class="flex items-center gap-2">
          <span class="inline-flex h-9 w-9 items-center justify-center rounded-lg bg-blue-600 text-lg font-semibold text-white">RCI</span>
          <div>
            <p class="text-base font-semibold text-slate-900">Rails CI</p>
            <p class="text-sm text-slate-500">Focused view for the Rails build matrix</p>
          </div>
        </div>
        <nav class="text-sm text-slate-500">
          <a href="https://github.com/rails/rails" class="hover:text-slate-700" target="_blank" rel="noreferrer">rails/rails</a>
        </nav>
      </div>
    </header>

    <div class="mx-auto max-w-4xl px-6">
      <% flash.each do |level, message| %>
        <div class="mt-6 rounded-md border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">
          <strong class="font-semibold"><%= level.to_s.titleize %>:</strong>
          <span class="ml-1"><%= message %></span>
        </div>
      <% end %>
    </div>

    <main class="mx-auto max-w-6xl px-6">
      <%= yield %>
    </main>
  </body>
</html>
